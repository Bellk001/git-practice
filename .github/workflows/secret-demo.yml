name: Secret Demo

on: push

jobs:
  show-secret-usage:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secret safely
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: echo "Connecting to database with a password of length ${#DB_PASSWORD} characters."